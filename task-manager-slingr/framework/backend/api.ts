import { Repository } from "./db";
import { Schema } from "./schemas";
import { ObjectActionDefinition } from "./actions";

export function addSchema<T>(schema: Schema, repository: Repository<T>) {
    
}

export function addAction<S, P>(action: ObjectActionDefinition<S, P>) {
    
}