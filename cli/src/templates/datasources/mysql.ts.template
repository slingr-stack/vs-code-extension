import { TypeORMSqlDataSource } from 'slingr-framework'

export const mysqlDataSource = new TypeORMSqlDataSource({
    type: 'mysql',
    host: 'localhost',
    port:  3306,
    username: 'root',
    password: 'root',
    database: '{{APP_NAME}}',
    synchronize: true,
    logging: false,
    managed: true
})